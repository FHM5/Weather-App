name: portfolio_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^10.1.0
  fl_chart: ^0.36.0
  math_expressions: ^2.2.0  # Add this if you want to evaluate complex expressions
  http: 1.2.1
  geolocator: ^12.0.0
  url_launcher: ^6.0.20
  image_picker: ^0.8.4+4
  file_picker: ^5.0.1  

dev_dependencies:
  flutter_test:
    sdk: flutter

